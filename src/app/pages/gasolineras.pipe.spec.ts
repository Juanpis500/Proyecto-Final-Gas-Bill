import { GasolinerasPipe } from './gasolineras.pipe';

describe('GasolinerasPipe', () => {
  it('create an instance', () => {
    const pipe = new GasolinerasPipe();
    expect(pipe).toBeTruthy();
  });
});
